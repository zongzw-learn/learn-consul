#!/bin/bash

# call nginx cluster's dynamic upstream api to:

    # get original upstream server list with S included

        # if S not exists and need to add: add it.

        # if S exists and need to remove: remove it.